interface:
  display_name: "ScopeVault"
  short_description: "Repo-specific patterns and workflows for ScopeVault"
  default_prompt: "Use the ScopeVault repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true