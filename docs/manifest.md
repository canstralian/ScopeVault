# Source Material Manifest

This folder preserves the uploaded source material used to derive the repo skeleton.

- Methodology summaries (`*.txt`)
- Program and reporting notes (`*.txt`, `*.docx`)
- Architecture and implementation prompt for the evidence server (`implement-the-evidence-server-next.docx`)
- Strategy and org design docs (`*.docx`)
